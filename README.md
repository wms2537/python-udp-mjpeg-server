# Python udp socket mjpeg server

Python tornado udp server which accepts mjpeg stream from clients and display on browser.

## Getting Started
Start server and view stream from browser `http://localhost:3000`